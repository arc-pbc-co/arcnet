{
  "buildCommand": "cd arcnet-console && npm install && npm run build",
  "outputDirectory": "arcnet-console/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
