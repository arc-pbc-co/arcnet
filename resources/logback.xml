<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ARCNet namespaces -->
    <logger name="arcnet" level="DEBUG"/>

    <!-- Security - never log at TRACE to avoid secret exposure -->
    <logger name="arcnet.security" level="INFO"/>
    <logger name="arcnet.config" level="INFO"/>

    <!-- Kafka client - reduce noise -->
    <logger name="org.apache.kafka" level="WARN"/>

    <!-- Bouncy Castle -->
    <logger name="org.bouncycastle" level="WARN"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
