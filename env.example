# =============================================================================
# ARCNet Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values.
# NEVER commit .env to version control.
# =============================================================================

# Environment: dev, staging, or prod
ARCNET_ENV=dev

# Node Identity
ARCNET_NODE_ID=node-001
ARCNET_GEOZONE_ID=geozone-west

# Kafka Configuration
ARCNET_KAFKA_BOOTSTRAP_SERVERS=localhost:9092
ARCNET_KAFKA_USERNAME=arcnet-client
ARCNET_KAFKA_PASSWORD=change-me-in-production

# Optional Kafka overrides
# ARCNET_KAFKA_CLIENT_ID=custom-client-id
# ARCNET_KAFKA_GROUP_ID=custom-group-id

# TLS Keystore (client identity for mTLS)
ARCNET_KEYSTORE_PATH=/path/to/keystore.p12
ARCNET_KEYSTORE_PASSWORD=keystore-password

# TLS Truststore (trusted CA certificates)
ARCNET_TRUSTSTORE_PATH=/path/to/truststore.p12
ARCNET_TRUSTSTORE_PASSWORD=truststore-password

# Production-only: External certificate paths
# ARCNET_ROOT_CA_PATH=/etc/arcnet/certs/root-ca.pem
# ARCNET_GEOZONE_CA_PATH=/etc/arcnet/certs/geozone-ca.pem
# ARCNET_NODE_CERT_PATH=/etc/arcnet/certs/node.pem
# ARCNET_NODE_KEY_PATH=/etc/arcnet/certs/node-key.pem
